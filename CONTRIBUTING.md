# 贡献指南

感谢你愿意参与 dshed。项目虽然小，但欢迎一切真实贡献：修 bug、补文档、报问题、提需求。

**重要**：所有 Issue 与 PR 均由人类维护者审阅并手动处理。我们不使用任何自动化提交或合并（包括 AI 自动生成的 PR / Issue）。你提交的内容会被认真 review，可能被要求修改，这是正常流程。

## 提交 Issue

Issue 是项目最直接的贡献方式。提交前请先搜索是否已有相似 Issue。

### Bug 报告

标题请概括现象（如"macOS 14 上托盘图标不显示"），正文包含：

- **环境**：平台（macOS/Windows/Linux + 版本）、架构（arm64/x64）、dshed 版本
- **复现步骤**：具体操作路径
- **预期行为**：应该怎样
- **实际行为**：发生了什么（附截图/日志更好）

### 功能请求

- 说明使用场景与要解决的问题（"为什么要"，而非只写"做什么"）
- 保持范围可讨论：小功能可以直接提，大改动建议先讨论再实现

## 提交 Pull Request

### 前置检查

- Fork 仓库，基于 `main` 创建分支（`fix/xxx` 或 `feat/xxx`）
- 改动保持**范围克制**：一次 PR 解决一个问题
- 本地跑通 `npm test`
- 不包含生成产物（`dist/`、`resources/` 等）、不包含任何密钥或个人信息

### 提交信息

遵循 [Conventional Commits](https://www.conventionalcommits.org/)，中文说明：

```
<type>(<scope>): <中文描述>
```

- type：`feat` / `fix` / `docs` / `refactor` / `build` / `style` / `revert`
- 描述用动宾结构，一眼看出改动意图（如 `fix(tray): 修复 macOS 托盘图标不显示`）

### Review 流程

1. PR 提交后由维护者 review，可能请求修改
2. 所有对话解决后由维护者**手动合并**（不使用 squash-bot 自动操作）
3. 合并前请确保提交历史干净（无调试残留、无多余文件）

### 不会接受的 PR

- 修改生成产物或打包输出
- 引入与项目无关的依赖
- 未经讨论的大规模重构
- 包含敏感信息（密钥、个人路径、邮箱等）

## 行为准则

- 保持友善与专业，对不同意见用事实讨论
- 尊重维护者的决定，有异议可再次提出但不要纠缠
- 涉及安全问题的报告请私信维护者，而非公开

## 致谢

所有被合并的贡献者都会在 Release 说明中被点名致谢。
